<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SteeringMariaMaui.BaseSettingPage">
			 
    <ScrollView>
        <VerticalStackLayout 
            Spacing="25" 
            Padding="30,0" 
            VerticalOptions="Center">

            <Image
                Source="maria1.png"
                SemanticProperties.Description="Maria"
                HeightRequest="200"
                HorizontalOptions="Center" />

            <HorizontalStackLayout
            Spacing="25" 
            Padding="30,0" 
            >

            <Label 
                Text="Base Url"
                HorizontalOptions="Start" />

            <Entry x:Name="BaseUrl"
              Placeholder="Enter text"
              TextChanged="OnEntryTextChanged"
              Completed="OnEntryCompleted" />
            </HorizontalStackLayout>
            <HorizontalStackLayout
            Spacing="25" 
            Padding="30,0" 
            >
            <Label 
                Text="Shut down ship computer"
                HorizontalOptions="Start" />
            <ImageButton Source="powerbutton.png"  
                Clicked="OnPowerClicked" 
                HorizontalOptions="Center" 
                VerticalOptions="Center" >
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                           
                        </VisualState>
                        <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Scale"
                                Value="0.8" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="PointerOver" >
                            <VisualState.Setters>
                            <Setter Property="Scale"
                                Value="1" />
                        </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
            </ImageButton>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
 
</ContentPage>
